<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.ThemeOverlay.AppCompat.Dark">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
    </style>


    <style name="traceroute_property">
        <item name="android:padding">5dp</item>
    </style>

    <style name="traceroute_property_header">
        <item name="android:textSize">20dp</item>
        <item name="android:textColor">@color/lightTextColor</item>
    </style>

    <style name="traceroute_property_value">
        <item name="android:textSize">16dp</item>
        <item name="android:textColor">@color/lightTextColor</item>
    </style>

    <style name="brand_button">
        <item name="android:background">@drawable/shape_button_accent_color</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_margin">10dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/lightTextColor</item>
        <item name="android:textSize">16dp</item>
    </style>

</resources>